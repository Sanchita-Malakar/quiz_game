// prisma/schema.prisma

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql" // Changed from sqlite
}

model Quiz {
  id        String   @id @default(cuid())
  title     String
  author    String
  totalTime Int
  userId    String   // NEW: Store the Auth User ID (UUID) for RLS
  createdAt DateTime @default(now())
  questions Question[]
  results   PlayerResult[]
}

model Question {
  id        String   @id @default(cuid())
  quizId    String
  quiz      Quiz     @relation(fields: [quizId], references: [id])
  text      String
  image     String?  // Stores Base64 string
  options   Option[]
}

model Option {
  id         String   @id @default(cuid())
  questionId String
  question   Question @relation(fields: [questionId], references: [id])
  text       String
  isCorrect  Boolean
}


model PlayerResult {
  id         String   @id @default(cuid())
  quizId     String
  quiz       Quiz     @relation(fields: [quizId], references: [id])
  userId     String?  // ← Remove the ? from the field name
  playerName String
  score      Int
  total      Int
  timeTaken  Int
  avatar     String
  createdAt  DateTime @default(now())
}